# 🤖 دستیار هوشمند تنظیم جلسه
## Smart Meeting Assistant

این پروژه یک دستیار هوشمند برای تنظیم خودکار جلسات است که به صورت ترکیبی از یک ربات تلگرام و یک کلاینت تلگرام (با استفاده از Telethon) عمل می‌کند.

## 🎯 ایده پروژه

کاربر در حال چت کردن با دیگران در تلگرام است و در مکالمات خود به شکل طبیعی درباره «جلسه»، «قرار» یا «meeting» صحبت می‌کند. این سیستم این پیام‌ها را شناسایی می‌کند و بدون اینکه کاربر نیاز داشته باشد دستی وارد تقویم شود، برای او یک جلسه در Google Calendar ثبت می‌کند.

## 🏗️ ساختار پروژه

```
project/
│
├── bot/                  # ماژول ربات تلگرام
│   └── main.py          # ربات اصلی با aiogram
│
├── telethon_client/     # اتصال به تلگرام کاربر با Telethon
│   ├── manager.py       # مدیریت کلاینت‌های Telethon
│   └── sessions/        # فایل‌های session کاربران
│
├── calendar/            # ارتباط با Google Calendar
│   └── calendar_api.py  # API تقویم گوگل
│
├── database/            # دیتابیس SQLite
│   └── db.py           # مدیریت دیتابیس
│
├── logs/               # فایل‌های لاگ
├── main.py             # فایل اصلی اجرا
├── requirements.txt    # وابستگی‌ها
└── config.env.example  # نمونه فایل تنظیمات
```

## 🚀 ویژگی‌ها

### بخش اول: ربات تلگرام
- ✅ تعامل اولیه با کاربر
- ✅ گرفتن شماره تلفن
- ✅ راه‌اندازی فرآیند اتصال به حساب تلگرام
- ✅ استفاده از کتابخانه `aiogram`
- ✅ پیام خوش‌آمدگویی و توضیح عملکرد
- ✅ دکمه اتصال حساب تلگرام
- ✅ درخواست شماره تلفن با `request_contact`

### بخش دوم: کلاینت Telethon
- ✅ اتصال به حساب تلگرام واقعی کاربر
- ✅ مانیتورینگ پیام‌های دریافتی
- ✅ تشخیص کلمات کلیدی: جلسه، قرار، meeting
- ✅ ذخیره session در فایل
- ✅ پردازش کد تأیید

### بخش سوم: Google Calendar
- ✅ اتصال به Google Calendar API
- ✅ ثبت خودکار جلسات
- ✅ استخراج زمان از پیام‌ها
- ✅ ارسال لینک جلسه به کاربر

## 📋 پیش‌نیازها

- Python 3.8+
- حساب تلگرام با API ID و API Hash
- حساب Google با دسترسی به Calendar API
- Service Account برای Google Calendar

## 🛠️ نصب و راه‌اندازی

### 1. کلون کردن پروژه
```bash
git clone <repository-url>
cd meeting-assistant
```

### 2. نصب وابستگی‌ها
```bash
pip install -r requirements.txt
```

### 3. تنظیم متغیرهای محیطی
فایل `config.env.example` را کپی کرده و به `.env` تغییر نام دهید:

```bash
cp config.env.example .env
```

سپس مقادیر زیر را در فایل `.env` تنظیم کنید:

```env
# تنظیمات ربات تلگرام
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_API_ID=your_api_id_here
TELEGRAM_API_HASH=your_api_hash_here

# تنظیمات Google Calendar
GOOGLE_SERVICE_ACCOUNT_FILE=service_account.json
GOOGLE_CALENDAR_ID=your_calendar_id_here

# تنظیمات دیتابیس
DATABASE_PATH=database/users.db

# تنظیمات عمومی
LOG_LEVEL=INFO
SESSION_DIRECTORY=telethon_client/sessions
```

### 4. تنظیم Google Calendar

1. به [Google Cloud Console](https://console.cloud.google.com/) بروید
2. پروژه جدید ایجاد کنید یا پروژه موجود را انتخاب کنید
3. Calendar API را فعال کنید
4. Service Account ایجاد کنید
5. کلید JSON را دانلود کرده و در پوشه پروژه قرار دهید
6. تقویم خود را با ایمیل Service Account به اشتراک بگذارید

### 5. اجرای برنامه
```bash
python main.py
```

## 📱 نحوه استفاده

### برای کاربران:

1. **شروع**: `/start` را به ربات ارسال کنید
2. **اتصال**: روی "اتصال حساب تلگرام" کلیک کنید
3. **شماره**: شماره تلفن خود را ارسال کنید
4. **کد**: کد تأیید دریافتی را وارد کنید
5. **مانیتورینگ**: حالا سیستم پیام‌های شما را مانیتور می‌کند

### تشخیص جلسات:

سیستم کلمات زیر را تشخیص می‌دهد:
- جلسه
- قرار
- meeting
- appointment
- session
- میتینگ
- ملاقات
- دیدار
- نشست
- کنفرانس

### مثال پیام‌های قابل تشخیص:
```
"فردا ساعت 14 جلسه داریم"
"قرار ملاقات ساعت 10:30"
"meeting tomorrow at 3 PM"
"نشست تیم ساعت 16"
```

## 🔧 تنظیمات پیشرفته

### تغییر کلمات کلیدی:
در فایل `telethon_client/manager.py`:
```python
self.meeting_keywords = [
    'جلسه', 'قرار', 'meeting', 'appointment',
    # کلمات جدید اضافه کنید
]
```

### تنظیم الگوهای زمانی:
```python
self.time_patterns = [
    r'(\d{1,2}):(\d{2})',  # 14:30
    r'ساعت\s*(\d{1,2})',   # ساعت 14
    # الگوهای جدید اضافه کنید
]
```

## 📊 دیتابیس

پروژه از SQLite استفاده می‌کند و شامل جداول زیر است:

- **users**: اطلاعات کاربران
- **detected_messages**: پیام‌های شناسایی شده
- **calendar_events**: جلسات ثبت شده

## 🔒 امنیت

- اطلاعات session هر کاربر به صورت مجزا ذخیره می‌شود
- هیچ رمز یا توکن حساسی در کد hardcode نشده
- دسترسی به Telethon فقط پس از تایید صریح کاربر
- تمام لاگ‌ها در فایل‌های جداگانه ذخیره می‌شوند

## 🐛 عیب‌یابی

### مشکلات رایج:

1. **خطای اتصال به Google Calendar**:
   - بررسی کنید Service Account صحیح باشد
   - تقویم با ایمیل Service Account به اشتراک گذاشته شده باشد

2. **خطای اتصال Telethon**:
   - API ID و API Hash صحیح باشند
   - شماره تلفن همان شماره تلگرام باشد

3. **ربات پاسخ نمی‌دهد**:
   - Token ربات صحیح باشد
   - ربات در گروه‌ها اضافه شده باشد

## 📝 لاگ‌ها

فایل‌های لاگ در پوشه `logs/` ذخیره می‌شوند:
- `app.log`: لاگ اصلی برنامه
- لاگ‌های جداگانه برای هر ماژول

## 🤝 مشارکت

برای مشارکت در پروژه:
1. Fork کنید
2. Branch جدید ایجاد کنید
3. تغییرات را commit کنید
4. Pull Request ارسال کنید

## 📄 مجوز

این پروژه تحت مجوز MIT منتشر شده است.

## 📞 پشتیبانی

اگر مشکلی داشتید یا سوالی دارید:
- Issue در GitHub ایجاد کنید
- با تیم توسعه تماس بگیرید

---

**نکته**: این پروژه برای اهداف آموزشی و نمایشی ایجاد شده است. در استفاده تجاری، حتماً مسائل امنیتی و حریم خصوصی را در نظر بگیرید.
