# 🚀 راهنمای نصب سریع
## Quick Installation Guide

## مرحله 1: نصب وابستگی‌ها
```bash
pip install -r requirements.txt
```

## مرحله 2: تنظیم متغیرهای محیطی
```bash
cp config.env.example .env
```

سپس فایل `.env` را ویرایش کنید و مقادیر زیر را تنظیم کنید:

```env
TELEGRAM_BOT_TOKEN=your_bot_token_here
TELEGRAM_API_ID=your_api_id_here
TELEGRAM_API_HASH=your_api_hash_here
GOOGLE_SERVICE_ACCOUNT_FILE=service_account.json
GOOGLE_CALENDAR_ID=your_calendar_id_here
```

## مرحله 3: تنظیم Google Calendar
1. فایل `service_account.json` را از Google Cloud Console دانلود کنید
2. آن را در پوشه اصلی پروژه قرار دهید
3. تقویم خود را با ایمیل Service Account به اشتراک بگذارید

## مرحله 4: اجرای تست
```bash
python test.py
```

## مرحله 5: اجرای برنامه
```bash
python main.py
```

## 🔧 تنظیمات اضافی

### ایجاد ربات تلگرام:
1. به @BotFather در تلگرام پیام دهید
2. دستور `/newbot` را ارسال کنید
3. نام و username ربات را انتخاب کنید
4. Token دریافتی را در فایل `.env` قرار دهید

### دریافت API ID و Hash:
1. به https://my.telegram.org بروید
2. با شماره تلفن خود وارد شوید
3. API ID و Hash را دریافت کنید

### تنظیم Google Calendar:
1. به https://console.cloud.google.com بروید
2. پروژه جدید ایجاد کنید
3. Calendar API را فعال کنید
4. Service Account ایجاد کنید
5. کلید JSON را دانلود کنید

## ⚠️ نکات مهم

- شماره تلفن باید همان شماره تلگرام باشد
- تقویم باید با ایمیل Service Account به اشتراک گذاشته شود
- تمام فایل‌های تنظیمات باید در پوشه اصلی پروژه باشند

## 🆘 در صورت مشکل

1. فایل لاگ را بررسی کنید: `logs/app.log`
2. تست‌ها را اجرا کنید: `python test.py`
3. تنظیمات را دوباره بررسی کنید
4. Issue در GitHub ایجاد کنید
